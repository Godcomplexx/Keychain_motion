<resources>
    <style name="AppTheme" parent="android:style/Theme.Material.NoActionBar">
        <!-- The panel supplies its own surface, so the window behind it is the
             dark backdrop it floats on. -->
        <item name="android:fontFamily">monospace</item>
        <item name="android:colorAccent">#D8402F</item>
        <item name="android:windowBackground">#201F1B</item>
        <item name="android:statusBarColor">#201F1B</item>
        <item name="android:navigationBarColor">#201F1B</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
